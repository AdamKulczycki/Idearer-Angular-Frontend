export class Article {
    constructor(public id: number, public title: string,
        public content: string, public created: string, public likes: number, public author: string, public category: string) {}
}
